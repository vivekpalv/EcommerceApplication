# Application
spring.application.name=amazon
server.port=8080

# Database
spring.datasource.url=jdbc:mysql://localhost:3306/amazon?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=vivekpal
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT
jwt.secret=EcommerceApplicationEcommerceApplication

# MultiPart-File
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Logging
logging.file.name=amazon.log